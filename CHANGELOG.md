# Changelog

## 1.0.0 — 2026-08-23

首个版本。

**核心命题**：BlueInk 不是汽车文案规则库，而是一套证据驱动的编辑决策系统。它不把历史材料直接当规则，而是根据当前任务重新判断事实边界、传播主线、素材取舍和信息权重，动态形成一份只对本次有效的写作程序，再交给互相隔离的写作与审查职责执行。

### 入口与责任

- 唯一入口 `/blueink-suite`，`SKILL.md` 与 `commands/` 两处都关闭模型自动调用，`validate.py` 硬检查。
- 启动回执：收到调用后第一句必须回显 `run-id`、品牌、老师与模式，让"这次有没有真的走技能"一眼可判。
- 主智能体只对话、路由、裁决；六个责任型子智能体分别负责取证、编辑策略、成稿、来源核验、编辑反方与反馈归因，调用图只有一层。
- 工具白名单收到最小集：只有证据研究员有检索工具；`install.sh` 默认把六个角色注册为原生子智能体，让边界由工具层强制而不是靠自律。

### 单品牌与单人隔离

- 一个工作空间绑定一个品牌知识库和一位文案老师；源库只读，派生物全部落在项目内 `.blueink/`。
- `bind` 拦截绑到品牌集合层的情况，判定用结构启发式而非品牌名表——新增品牌不需要改技能。
- `bind --create`：知识库目录还不存在时按标准语料布局建出来并写好配置，不要求老师先自己整理。
- `check-brand` 与 `open --brand`：本次要写的品牌与绑定的知识库不匹配时拒绝并给出三条出路，不静默选边。
- 老师直接给出参考文件路径时可以完全不用知识库：未绑定项目也能以附件为唯一证据边界开启运行，启动回执写明"无知识库·以附件为准"。

### 证据与检索

- 旁路增量索引：只读扫描，按内容哈希增量，schema 变更自动全量重建，`--full` 可手动重建，`--limit` 是不落盘的预览。
- 索引不追求一次分类正确：低置信度文件保留多个候选标签，只有影响当前稿件时才在访谈里问。
- 知识库里混进来的写作技能包按**目录**整棵子树隔离，默认不参与检索——固定模板往往不在入口文件里，而在它旁边的 `references/` 目录里。
- DOCX / PPTX 解包取正文；PDF、早期 Office 格式与图片只登记元数据，并明确标记正文未被读取。
- 官方来源白名单由 `official.check_url` 单点判定，联网取证前必须通过；白名单为空时一律拒绝。
- 老师本次提供的附件不受绑定根限制，但必须登记路径与内容哈希，否则与越界读取在证据上不可区分。

### 访谈与编辑判断

- 逐轮访谈，每轮恰好一个问题；问前先读已有回答、看附件、查索引。
- 动态充分性判据是五个维度：事实安全、传播主线、信息权重、表达边界、交付可行性。它们是判据不是问卷。
- 七个开放决策域只作内部认知地图，不输出给老师，也不在访谈记录里逐项填状态。
- 不显示信心百分比。
- 后台始终生成结构化写作程序；只有出现新增高影响判断时才展示极短决策卡并暂停。
- 默认交付一稿；A/B 按不确定性条件触发，且差异必须发生在主线、素材取舍或信息预算层面。

### 学习与定位

- 反馈按证据强度、独立事件与反例更新条件化记忆的置信度；四级作用域，方法论级只记录不自动生效。
- 六项验收契约（A1—A6）编译成机械审计器，把违约定位到具体角色、文件与句子。
- 运行记录保留 90 天且至少保留最近 20 次；`purge` 默认试运行，范围永不触及源知识库。

### 验证

- 状态层回归 158 项。
- 审计夹具 12 组 × 11 项命令型检查；其中一组从真实运行脱敏而来，设为保留测试不参与日常评分。
- 变异门：主动改坏实现，确认对应测试真的转红。
- 规范校验、轻量安全扫描、管线检查、评测契约校验。

### 不包含

Word 排版、字体、配图、品牌文档模板、改稿编排与版本回退。第一版只负责文案生成。
