# 产品版本

## 4.2.1 — 2026-08-25

- 未绑定业务项目首次运行时，即使带附件也先询问单品牌历史稿件共同根目录；老师明确确认“本次只用附件”后可用 one-off 快线。
- 绑定库内最多三份不同事件的同品类终稿可作为风格参考，附件仍是事实边界；交付核对卡分开列事实来源与风格参考。

## 4.2.0 — 2026-08-25

- `delivery.md` 成为老师唯一修改入口，成稿后立即写入并由 `handoff` 交付；默认运行不再生成 `draft.md`。
- `close` 不再把核对信息拼进正文，改为单独生成 `delivery-check.md`，保存交付核对卡与实际来源。
- 新运行升级为 schema 5；schema 4 与 3.x 历史运行保持只读审计兼容。

## 4.1.1 — 2026-08-25

- 显式启动后由宿主禁用其他 Skill、Agent 和 Task，避免品牌写作助手旁路 BlueInk 运行合同。
- 最终回复的第一个可见区块固定为完整交付内容；完成提示、过程说明和归档路径只能放在全文之后。

## 4.1.0 — 2026-08-24

- 明确附件的普通生成改为初稿优先：轻量保存方向后直接成稿，不再把事实原子和完整编辑决策放在初稿前。
- 新增 `handoff`，立即展示完整可修改初稿并记录正文哈希、方向确认到初稿的实际耗时。
- 初稿交付后正文归老师修改；受限核验只记录发现的问题和局部替换建议，不自动覆盖正文。
- 附件快线的核验输入收敛为 `issues`；风险句由脚本机械提取，实际来源直接复用已登记附件。
- 时间只作为真实运行数据记录，不设置硬时限、自动超时或质量降级。

## 4.0.0 — 2026-08-24

- 默认生成合并为一份单智能体快线指导，研究、反馈和排障改为条件加载。
- 生成任务必须在成稿前完成动态写法确认；普通任务一到两轮，事实硬冲突可逐轮追加。
- 默认运行产物收敛为 `run.json`、`draft.md`、`verify.json`、`delivery.md`。
- 新增 `save` 命令，结构化输入即时校验并原子写入；`close` 自动生成实际交付内容。
- 高风险复核只检查事实、比较范围、因果和时效句，不再默认运行独立红队或多轮返工。
- 模型与 effort 保持用户会话选择，不进入 Skill 规格。

## 3.1.0 — 2026-08-24

BlueInk 是面向 Claude Code 的证据驱动汽车公关文案编辑决策系统。

### 产品能力

- 通过 `/blueink-suite` 显式启动，同名冲突时使用 `/blueink-suite:blueink-suite`。
- 一个工作空间绑定一个品牌知识库，索引与写作流程只读源库。
- 明确附件默认形成封闭证据边界，可在未绑定知识库时直接开始任务。
- 当前会话按需执行编辑策略、成稿、来源核验、编辑红队、证据研究与反馈归因。
- 封闭文本附件走轻量路径；知识库检索、官方补充、时效问题、来源冲突或缺口触发扩展证据研究。
- 每次任务生成独立的事实、策略、写作、核验和审查产物，支持断点恢复与问题定位。
- 反馈形成带触发条件、不适用范围、反例和置信度的候选记忆。

### 运行环境

- Claude Code only。
- macOS 与 Windows。
- Python 3.9 或更高，无第三方 Python 依赖。

### 验证范围

- 状态层回归：174 项。
- 审计夹具：13 组，含 2 组保留测试。
- 变异门覆盖 11 个高影响失败条件。
- 项目规范、安全扫描、管线检查、评测规格和 Claude Code 插件清单均有机械校验。

机械验证确认结构、状态和审计契约；文案质量由文案老师在真实任务中判断。
