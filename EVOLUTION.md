# 使用中捕获的纠错

这份文件是**追加式**的：`scripts/evolve.py --correct "<实际观察到的错误>"` 会把老师或维护者的原话原样写进来，作为待审证据。

它不是变更日志，也不是设计说明——它记录的是**在真实使用中被观察到、但还没有被消化成机制的东西**。

三条纪律：

- **原话照录，不改写。** 一条被润色过的纠错会丢掉最有用的部分：老师当时到底看到了什么。
- **只追加，不重写已有条目。** 把失败记录改掉比让记录过期更糟。
- **`--correct` 不会自动修改方法论。** 它只落一条证据。要不要变成机制，由维护者判断，走对照验证和对抗审查。

处理一条纠错的去处有三个，按范围选最窄的那个：

| 现象 | 去处 |
|---|---|
| 本地环境的反直觉事实、踩过的坑 | `references/troubleshooting.md` 的 Gotchas |
| 某条机械约束需要收紧或放宽 | 对应脚本的唯一执行点 + 一条回归测试 + `DESIGN_NOTES.md` 里写清判据 |
| 涉及核心命题或角色划分 | 先记为方法论候选，不直接改 `SKILL.md` |

---

（尚无条目。）
