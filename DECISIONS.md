# 产品决策

## 1. 显式入口

BlueInk 只由 `/blueink-suite` 或命名空间入口启动。普通自然语言不自动触发。

## 2. 单智能体

当前会话负责访谈、方向确认、成稿和高风险事实复核；不调用 Agent、Task、后台智能体或独立模型进程。同一上下文内的核验只称受限复核。

## 3. 成稿前确认

生成任务至少有一次写法确认。普通任务一到两轮；只有事实或来源硬冲突允许逐轮追加。写法选项来自本次主张、受众和信息权重，不使用固定风格菜单，不制造虚假 A/B。

## 4. 证据边界

老师显式附件默认形成封闭事实边界；自主检索只在绑定的单品牌知识库内。扩展研究只闭合已经命名的高影响缺口、冲突或时效问题。

## 5. 最小运行合同

默认生成只有 `run.json`、`draft.md`、`verify.json`、`delivery.md`。结构化文件由 `blueink.py save` 校验并原子写入，正文由当前智能体直接写，`close` 生成交付内容。

## 6. 事实表达

事实原子最多 12 条。`唯一、全部、普遍、均、最高` 必须有明确比较范围与来源授权；否则正文使用有限定条件的事实或分析判断。

## 7. 条件能力

`generate.md` 是唯一默认运行指导。研究、反馈和排障各自按条件读取。编辑红队不再默认单独运行；敏感内容或高影响推断最多把三条问题写入核验记录。

## 8. 能力边界

负责文案生成和来源核对，不承担改稿编排、版本回退、Word 排版、品类认证或自然语言自动触发。模型和 effort 由用户会话选择。
