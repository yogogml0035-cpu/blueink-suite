# 产品架构合同

这份文件定义 BlueInk 的当前产品结构与不可破坏的设计边界。

## 1. 单一显式入口

根级 `SKILL.md` 是唯一运行入口，并设置 `disable-model-invocation: true`。默认命令为 `/blueink-suite`，同名冲突时使用 `/blueink-suite:blueink-suite`。

`open` 创建包含 `run_id` 与 `started_via` 的 `meta.json`，审计 A1 根据运行记录验证入口。

## 2. 单一 Skill、单一运行会话

BlueInk 以一个完整 Skill 交付。访谈、证据、策略、写作、核验、红队和反馈共享同一任务状态，由当前 `/blueink-suite` 会话按需执行。

插件根目录不包含 `agents/`。阶段指导位于 `references/stages/`，不作为独立入口注册。

## 3. 四个核心阶段、两个条件阶段

生成主干包含编辑策略、成稿、来源核验和编辑红队。扩展证据研究只在需要附件外资料时进入；反馈归因只在老师提供真实修改或反馈时进入。

登记附件正文可读、足以支撑交付、没有高影响冲突或时效问题，也不需要知识库或官方补充时，编辑策略阶段先形成轻量 `evidence.json`，再形成 `strategy.json`。

## 4. 方法与品牌知识分离

技能保存方法、阶段职责和状态协议。每个项目通过 `.blueink/workspace.yaml` 绑定一个品牌知识库。新增品牌使用独立项目，品牌事实与表达不写入技能本体。

`bind --create` 可创建空知识库骨架；绑定后的索引与写作流程只读源库。`index/` 可重建，`learning/` 与 `runs/` 包含不可自动恢复的信息。

## 5. 附件与自主检索分开管理

用户明确提供的附件是本次任务的授权输入，不受绑定根限制，但必须在 `open --attach` 时登记路径、内容哈希、品牌归属和来源。

绑定根只约束系统自主检索。附件充分时走封闭附件快线；高影响缺口、冲突、时效问题或官方补充需求触发最小范围扩展。

## 6. 语义判断与确定性边界分工

访谈、证据解释、编辑策略、写作和语义审查由当前会话完成。路径校验、内容哈希、增量索引、URL 白名单、运行记录、置信度计算与契约审计由标准库脚本完成。

每条机械约束只有一个执行点：路径归属由 `workspace.within` 判断，官方来源由 `official.check_url` 判断，阶段边界由 `audit.check_stages` 判断。

## 7. 阶段产物是运行合同

运行目录使用 `evidence.json`、`strategy.json`、`program.json`、`draft.md`、`write-receipt.json`、`verify.json`、`adversary.json` 和 `feedback.json` 记录阶段结论。

每份产物先落盘再进入下一阶段。事实问题回到证据整理，主线问题回到编辑策略，执行偏差回到成稿。恢复时从最后一个缺失、损坏或 `blocked` 的产物继续。

## 8. 来源与编辑采用双重复核

来源核验只依据正文、证据包和写作程序，判断 `matched`、`drifted`、`unsourced`、`stale`、跨品牌污染和红线命中。

编辑红队只依据正文、传播任务和交付合同，检查传播目标、主线、结构、信息权重、品牌感和特殊条件。

两项复核由同一会话执行，产品将其标记为受限输入复核。

## 9. 学习使用条件化记忆

反馈先形成候选知识，并记录证据强度、触发条件、不适用范围、反例和置信度。单次修改不直接成为通用规则；方法论候选只记录，不由品牌工作空间自动生效。

## 10. 评测验证产品合同

自动评测覆盖入口、单品牌隔离、动态访谈、阶段边界、输出有效、状态层回归、声明一致性、变异承重和管线接线。

文案质量、编辑归因准确性和真实业务采用效果由文案老师判断，不使用自动评分代替。

## 11. 能力边界

BlueInk 负责文案生成，不承担 Word 排版、字体、配图、品牌文档模板、品类认证、改稿编排或版本回退。

产品只支持 Claude Code，并要求显式入口；不承诺自然语言自动触发。

## 12. 分发边界

仓库提供 Claude Code 插件清单、macOS 安装脚本和 Windows 安装脚本。安装由用户或安装 Agent 明确执行，开发与验证流程不写入用户的 Claude 环境。
