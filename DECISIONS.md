# 产品决策

## 1. 显式入口

BlueInk 只由 `/blueink-suite` 或命名空间入口启动。普通自然语言不自动触发。

## 2. 单 Skill、单智能体

BlueInk 是本次唯一运行 Skill；入口在宿主层禁用 Skill、Agent、Task，显式启动后不再加载其他 Skill、斜杠命令或品牌写作助手。当前会话负责访谈、方向确认、成稿和高风险事实复核，不调用后台智能体或独立模型进程。同一上下文内的核验只称受限复核。

## 3. 成稿前确认

生成任务至少有一次写法确认。普通任务一到两轮；只有事实或来源硬冲突允许逐轮追加。写法选项来自本次主张、受众和信息权重，不使用固定风格菜单，不制造虚假 A/B。

## 4. 证据边界

业务项目首次使用时先绑定一个不包含其他品牌的历史稿件共同根目录；即使本次带附件也不能静默跳过，只有老师明确确认“本次只用附件”才走一次性任务。老师显式附件默认形成封闭事实边界；绑定库内最多三份不同事件的同品类终稿只提供风格参考，不补充事实。扩展研究只闭合已经命名的高影响缺口、冲突或时效问题。

## 5. 最小运行合同

默认生成只有一份正文：`delivery.md`。`run.json` 和 `verify.json` 保存机器状态，`delivery-check.md` 单独保存交付核对卡与实际来源。方向确认后先写完整稿件并用 `handoff` 交给老师；核验只记录问题和局部建议，不自动覆盖正文。`close` 不重写正文，只生成核对侧车并归档。

## 6. 事实表达

附件快线不在成稿前重复整理事实原子；扩展研究最多保留 12 条。`唯一、全部、普遍、均、最高` 等比较表达必须进入初稿后的高风险复核；来源不支持就给出有限定条件的替换建议。

## 7. 条件能力

`generate.md` 是唯一默认运行指导。研究、反馈和排障各自按条件读取。编辑红队不再默认单独运行；敏感内容或高影响推断最多把三条问题写入核验记录。

## 8. 能力边界

负责文案生成和来源核对，不承担改稿编排、版本回退、Word 排版、品类认证或自然语言自动触发。模型和 effort 由用户会话选择。

## 9. 时间口径

记录方向保存、正文交付、核验和核对卡生成的实际时间，重点观察方向确认到可修改正文的耗时。不设置硬时限、自动超时或为了速度牺牲质量的降级路径。
