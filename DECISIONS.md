# 架构决策

每条记录：决定了什么、为什么、**被否掉的替代方案是什么**。没有替代方案的"决策"只是描述。

## 1. 一个入口，不拆成可独立触发的品类技能

六个角色共享同一份任务状态和证据包，任何一个都不能独立交付业务价值。所以是一个根 `SKILL.md` 加六个子智能体定义。

**否掉的方案：按品类拆成多个可独立触发的技能。** 后果是自然语言可能绕过总入口命中某个子技能，于是"看起来在写稿、其实走的是另一条链路"。品类只保留任务义务与边界，不保留固定段落模板。

**否掉的方案：六个可独立安装的组件。** 责任隔离靠的是调用图只有一层，拆成独立组件会让"文件层层嵌套"变成"调用层层嵌套"。

## 2. 入口约束必须机械化，不能只写在正文里

根级 `SKILL.md` 是唯一入口，并设 `disable-model-invocation: true`。Claude Code 已原生支持插件根级单个 `SKILL.md`；再放一份同名 commands 入口会形成两个命令定义，解析顺序反而不可控。

**为什么不能只写正文。** 模型决定是否自动调用时读的是 frontmatter 的 `description`，正文要等调用之后才会被读到。把"不承诺自然语言自动触发"写在正文里，是在事后向已经进来的人解释规则。

`open` 创建带 `run_id` 与 `started_via` 的 `meta.json`。审计 A1 以运行记录核对显式入口，不依赖对话中的自报文本。

## 3. 方法与品牌知识分离，工作空间绑定单一品牌知识库

技能只保存方法、职责与状态协议。实际项目通过 `.blueink/workspace.yaml` 绑定一个品牌知识库。`bind --create` 是唯一显式写入知识库的初始化例外；绑定后的索引与写作流程只读语料。只有索引可重建，学习记忆和运行记录不是。

**否掉的方案：内嵌多个品牌的语料。** 不同品牌内容放在一起会造成检索误差，甚至把别的品牌加载进上下文；品牌终止合作或换领导时还会迫使技能本体随之变化。

**否掉的方案：为每个品牌做专门的运行包。** 那是按品牌拆 Agent 的变体，等于重造品牌技能包，新增品牌就要新增一套。

## 4. 认知责任交给 Agent，确定性边界交给脚本

访谈、证据解释、编辑策略、写作和语义审查需要模型判断。路径校验、内容哈希、增量索引、URL 白名单、运行记录、置信度升降、契约审计由标准库脚本执行。

判据是"这件事错了之后能不能被机械发现"。能，就应该是代码——因为提示词里的约束失败是静默的。

## 5. 一条约束只在唯一执行点实现一次

路径归属只在 `workspace.within`，官方来源只在 `official.check_url`，责任隔离只在 `audit.check_roles`。其余地方（Agent 契约、references、SKILL.md）只引用它，不各写一份判断逻辑。

**为什么。** 同一条约束在三个地方各实现一遍，就会有三种略微不同的行为，而且改一处不会同步另两处。散在各处的"安全检查"最终的效果是没人知道到底哪一处在生效。

## 6. 单一命令入口，但不伪造线性管线

`blueink.py` 是所有确定性工作的唯一入口（bind / status / index / retrieve / official / open / stage / close / purge / memory / audit / doctor）。

**它不是一条线性流水线。** 语义流程会根据访谈答案、证据缺口和冲突动态分支，那部分由主智能体按《编排协议》调度。收在一个入口里解决的是另一个问题：技能正文是散文，靠模型记住"先跑哪个脚本再跑哪个"不可靠。

## 7. 原生子智能体注册是默认路径，不是可选项

**否掉的方案：默认走 `general-purpose` + 首行读契约文件。** 这条路径在任何安装形态下都能用，看起来更稳。但它有一个致命性质：角色契约里的工具白名单**不被工具层强制执行**。写作者"没有检索工具"这句话在这条路径下不成立，越权只能靠审计事后从 `read_paths` 发现——发现得到，但那时稿子已经写完了。

所以只以 Claude Code 插件根级 `agents/` 注册角色；直装脚本复制完整插件，不向全局 agents 目录写第二份。`general-purpose` 仅作为插件角色不可用时的退化路径，并明确标注"边界只靠自律"。

## 8. 知识库里的历史技能包按目录隔离，不按文件名

**否掉的方案：按文件名过滤 `SKILL.md` / `AGENTS.md`。** 技能包里的固定模板不写在入口文件里，写在它旁边的 references 目录里。只拦入口文件，模板照样会在"新闻稿 结构"这类检索里排到前列——而那些模板正是"结构固化"这条反馈的来源文件。

判定方式：目录里出现 `SKILL.md` / `AGENTS.md` / `CLAUDE.md`，整棵子树都是指令产物。

**边界。** 隔离的是"由技能包生成的提示词与固定模板"，不是"经验总结"。老师手写的对接经验与传播红线仍是编辑策略轨的一等证据。

## 9. 品牌集合层拦截，且用结构启发式而非品牌名表

**否掉的方案：告警但放行。** 告警会被忽略，后果是整套稿子混用两套品牌表达。

**否掉的方案：硬编码已知品牌别名表。** 对第五个品牌就失效，而"新增品牌不改技能"是这套设计的承诺之一。硬编码品牌名也是一种形式的品牌知识内嵌。

采用的是通用结构启发式：子目录没有语料布局特征词、而是并列的若干名字就判为集合层。代价是偶发误判（需要 `--force`），但误拦一次的成本远低于漏拦一次。

## 10. 置信度上限 0.9，永不到 1.0

**否掉的方案：允许到 1.0。** 饱和到 1.0 会让"再来一个反例"在数值上失去意义。留出这 0.1 是为了让系统永远保持"这条也许不适用"的可能性。

同时：独立事件必须引用真实运行记录。**置信度完全依赖"多个独立任务中重复出现"这个计数**，不校验就等于允许凭空把偶发偏好推到自动生效。

反例的首选处置是**缩小适用范围**而不是降置信度到失效——降置信度只是让记忆变哑，缩范围才是把观察保留下来并说清它在哪里成立。

## 11. 评测对象是审计器与状态层，不是文案质量

**否掉的方案：用 llm-judge 评文案好不好。** 那等于把最难的判断交给一个不确定的环节，还额外骗了老师一次。文案质量只有文案老师能判断。

所以评测分两层：审计器的结构、自洽性、可定位性（13 个夹具 × 11 项命令型检查，含 2 个保留测试）；状态层的边界是否还守着（`test_state.py` 在临时目录里真的跑一遍绑定、索引、检索、URL 校验、记忆升降、审计、附件登记，173 项检查）。

配套两条纪律：**每项契约检查都必须有对应夹具**（否则零回归可能只是盲区）；**夹具之间的契约必须相互独立**（一项违约不能连带另一项，否则定位退化成"哪儿都有问题"）。

## 12. 能力边界是文案生成

不做 Word 排版、字体、配图、品牌文档模板、DOCX 渲染、客户端提交格式，也不建设改稿编排与版本回退。

这些属于"交付物生产"，不属于"文案生成方法论"。一起做会引入品牌模板、渲染、图片插入和跨平台兼容一大批工程问题，反而削弱对访谈、检索、编辑决策、写作和学习闭环的验证。确有需求时应做成独立的交付适配器，不改核心方法论和子智能体编排。

**注意区分两件事：** 老师后续的修改会被沉淀为候选证据（学习外循环），这不等于本次生成链路要重新运行（生成内循环）。本技能不建设完整的改稿路由与阶段回退 UI。

**被否掉的替代方案：给一张"修改意见性质 → 退到第几步"的对照表。** 完整的改稿编排需要版本、diff，以及段落与上游判断的追踪关系；本技能不具备这三项。**半个改稿系统比没有更糟——它会让人以为改稿已经被管起来。** 边界由 `self_check.py --claims` 机械守着：子命令表里出现 `rework` / `rollback` / `revert` 这类名字直接判失败。

## 13. 用户自行安装

包内保留 macOS `install.sh`、Windows `install.ps1` 与插件清单，但创建过程不执行安装。两个脚本只写 Claude Code 的个人 skills 目录，不建立跨宿主链接，也不另写全局 agents 目录。

## 14. 对外的每一句声明都必须有一道机械门

技能是散文，散文会漂移。高风险漂移有三类：声明支持 Python 3.9 但在 3.9 上崩、文档检查项数与实际不一致、能力边界与命令行子命令不一致。共同点是**它们都不报错**。

所以 `scripts/self_check.py` 守三件事：版本底线（`--compat`）、声明一致性（`--claims`）、检查自身的承重（`--mutation`）。

**被否掉的替代方案一：把这些写进发布检查清单，靠人核对。** 人会核对自己记得要核对的东西，而漂移恰好发生在没人记得的地方。

**被否掉的替代方案二：只做静态扫描，不跑真机。** 静态扫描只挡得住已登记的 API 名单。所以 `--compat` 在本机存在 3.9 解释器时会用它真跑一遍；找不到时**明确报"跳过"并写清代价**，不写成通过——把"没查"伪装成"查过了"是最危险的一种输出。

**被否掉的替代方案三：给规模设一条行数硬阈值。** 复杂度确实要控，但行数阈值是一道谁都学会绕过的门（拆文件即可）。改成两件更有力的事：`--claims` 要求**任何新脚本必须进入自研或外来件白名单**（复杂度失控是一次加一个没人登记的脚本），以及按四类报告规模构成——方法论与文档、自研脚本、可与上游重新同步的外来件、审计夹具。把性质不同的东西混成一个总行数，本身就是一种误导。README 只负责安装，不再承担维护清单。

## 15. 变异测试是一道门，不是一句话

"用变异测试确认过它承重"如果只是一句话，就没有证明力。**一段声称能抓住错误的检查，在它从没被喂过错误输入的情况下，和一段 `return True` 无法区分。**

所以 `--mutation` 每次都真的往技能副本里注入十个已知失败形态，断言每一个都让指定检查转红。**靶点找不到时判失败而不是跳过**——一个打不进去的变异什么都没测到，却会让这道门继续显示绿色。

`index-hash` 变异把增量索引改成按 `size + mtime` 判断复用。对应测试必须覆盖文件被同长度改写并恢复原修改时间的情形；否则"第二次索引全部复用"反而会给错误实现发通行证。这个失败会让文件内容已经变化、检索却永远拿不到新内容，且不报错。

**被否掉的替代方案：声明一个 canary 输入文件，规格校验时确认它存在。** 这只证明文件在那里，不证明有人喂过它。检查器有没有被喂过错误输入，只能靠真的喂一次。

## 16. 首读层是方法论，运行细节按事件加载

`SKILL.md` 的首读内容决定模型第一次接触这套东西时被什么占满注意力。方法论、命令、字段、审计规则和维护细节挤在同一屏里互相争夺注意力时，模型学到的是"这是一套要执行的流程"，而不是"这是一套要做的判断"，症状很具体：它会把七个开放决策域完整渲染成状态表输出给老师。所以首读层只放方法论，操作细节全部下沉到按需读取的 references。

所以首读层只保留一个核心命题、五条方法论原则、有向决策主干和五条运行底线，其余按当前正在闭合哪个判断按需读取。

**判据是一个反事实检验，不是长度。** 去掉 Claude Code、工作空间、Agent 这些技术名词之后，这条原则还能不能指导一个人写出更好的文案？能的属于方法论内核，不能的属于运行边界——后者仍然必须存在，但不该占据首读中心。

**被否掉的替代方案一：把细节删掉而不是搬走。** 那会把复杂度降下来，同时把最重要的能力删掉。已知价值最高的正是可追踪闭环。

**被否掉的替代方案二：把七个节点写成七项检查清单，反正模型爱看清单。** 清单会让"已经清楚的事再确认一次"变成默认动作，而这套方法论的第四条原则恰好是反过来的。

**验收标准不是行数。** 是首读内容聚焦方法论、reference 按事件正确加载、五项核心行为不退化。文件变短本身不证明方法论变好了。

## 17. 老师给的附件优先，绑定根只约束系统自主检索

跨品牌污染靠"自主检索根只有一个"来防。**但这条不该延伸成"拒绝老师本次明确提供的文件"。** 把两者混成一条规则的结果是运行逻辑与审计合同互相打架：主智能体允许读老师附加的跨根文件，审计规则把同一个路径判为越界。两个都不肯让步，于是每次都得手工解释一遍。

正确的切分是：**绑定根约束的是系统自己去找什么，不是老师授权它读什么。**

- 老师显式提供的附件是本次任务的证据，来源强度高于任何检索结果。
- 附件必须登记（`open --attach`）。**这是登记而不是审批**：不阻止任何路径，只让"老师给的"和"自己越界读的"在证据上可区分。没有它，审计要么放过真正的越界，要么把老师的授权判成违约。
- 老师说"以附件为准"时，附件默认构成封闭证据边界。只有出现会阻止成稿的高影响缺口时才最小范围扩展，并把缺口写出来。

**被否掉的替代方案一：允许审计跳过所有绝对路径。** 那等于取消 A2。跨品牌污染是最难缠的一类问题，不能为了一个附件场景把整条契约让掉。

**被否掉的替代方案二：把附件复制进知识库再读。** 这会写源库——"源库只读"是本技能最硬的一条承诺。而且复制一份就意味着以后有两份，其中一份会先过期。

**被否掉的替代方案三：把"未申报缺口就扩展检索"判为违约。** 真出现高影响缺口时扩展是对的，判违约会训练出"宁可漏事实也别扩展"的行为——那比多读几个文件糟得多。所以判可见提示：浪费必须看得见，但不必被禁止。
