# 设计说明与已知边界

这份文件回答两个问题：**每一条机械约束为什么存在**，以及**这套系统做不到什么**。

它不是操作手册（那在 `references/` 里），也不是变更日志（那在 `CHANGELOG.md` 里）。要判断某个新增设计该不该进来、或者要向别人解释某条约束凭什么值得存在时，读这里。

**一条原则贯穿全文：不给每个功能点堆一层安全治理。** 一条约束只在唯一执行点实现一次——路径归属在 `workspace.within`，品牌归属在 `workspace.brand_matches`，官方来源在 `official.check_url`，责任隔离在 `audit.check_roles`，动态充分性维度在 `audit.SUFFICIENCY_DIMENSIONS`。其余地方只引用它，不各写一份。两处实现同一条保证，迟早会分叉，而分叉的那一天没有人会发现。

---

## 一 · 入口：约束必须是机械的，不能是散文

验收契约第一条是"只能通过 `/blueink-suite` 显式启动"。把这句话写进技能正文是不够的——**正文要等调用之后才会被读到**。模型决定是否自动调用时，看的是 frontmatter 的 `description`，那里写"必须显式启动"只是一个软信号。

所以只保留根级 `SKILL.md`，并带 `disable-model-invocation: true`。Claude Code 当前支持插件根级单个 Skill；同名 commands 入口不是双保险，而是第二个入口。`validate.py` 会同时检查前者存在、后者不存在。

**启动回执是同一条约束的另一半。** 收到调用后第一句必须回显 `BlueInk 已启动 · run-id: …`。它让"这次到底有没有真的走技能"从一个感觉变成一眼可判、事后可查的事实。看不到这一行，就是没走技能。

验证方式：删掉 frontmatter 里的该字段，`validate.py` 立刻报 INVALID。

---

## 二 · 品牌隔离：靠单一检索根，不把提示词当边界

用提示词写"不要读取其他品牌的资料"是无效的：一次检索误差就够了，而且失败是静默的——它不报错，只让稿子在某几句话上变成另一个客户的口吻。

**一个工作空间只绑定一个品牌知识库，索引只扫描一个检索根。** 这条保证有前提：根目录本身只含该品牌，且没有用 `--force` 绕过集合层启发式。老师显式给出的附件也可以在根外，但必须登记；因此这里是“自主检索范围的机械边界”，不是对所有输入的物理隔离。

这条保证有三个执行点，各守一种失败形态：

| 执行点 | 守的是 | 判定方式 |
|---|---|---|
| `bind` 的集合层拦截 | 绑到了包含多个品牌的父目录 | 结构启发式：子目录里没有语料布局特征词、而是并列的若干名字 |
| `brand_matches` + `open --brand` | 本次要写的品牌不是绑定的那个 | 归一化后相等、`brand_key` 相等，或一方是另一方的子串 |
| `within` + 审计 A2 | 读取了绑定根之外、又没登记为附件的路径 | 真实路径归属判断，软链接按目标算 |

**集合层判定为什么不用品牌名表。** 硬编码的品牌名表对第五个品牌就失效，而"新增品牌不改技能"是这套设计的承诺之一。结构启发式对未登记过的品牌同样有效：两个并列的、名字里不含语料特征词的子目录，就是两个品牌。

**为什么是拦截而不是告警。** 告警会被忽略，而它的后果是整套稿子混用两套品牌表达。确认某个目录确实是单一品牌时用 `--force`，此时会明确提示跨品牌污染检查将因此失效。

**品牌名判定为什么放宽到子串。** 老师在访谈里习惯说简称。把"理想"判成与"理想汽车"不匹配，会让他每次都回答一次"是不是要换知识库"，而那个提示很快会被当成噪音跳过——一条被习惯性跳过的提示，比不存在更糟。反过来，「现代中国」与「现代汽车」互不为子串，仍然判为不匹配，而那正是需要拦的一类。

**符号链接不计入集合层判定。** 索引本来就不跟随符号链接，把它算成兄弟品牌会让知识库里一条无关的链接凭空触发拦截。**误拦比漏拦更糟**：它会逼人习惯性加 `--force`，而 `--force` 一旦成为肌肉记忆，这道闸就白装了。

验证方式：绑四品牌总根、绑"现代汽车"这一层、绑两个未登记过的品牌并列的目录，三种都被拦住；绑到具体品牌那一级正常通过；符号链接不触发误拦。

---

## 三 · 个人隔离：品牌隔离挡不住偏好互相污染

同一个品牌可能由两位老师分别负责不同稿件。条件化记忆学的是"这位老师在什么条件下通常如何判断"，两个人的偏好累加在同一条置信度上，学出来的是一个不存在的平均人。

症状是"我的偏好时灵时不灵"——最难查的一类问题，因为每条记忆单看都成立。

所以工作空间同时锁品牌和锁人：新绑定必须填写 `teacher`，品牌或老师改变时即使带 `--force` 也拒绝；每条记忆带老师归属，`memory list` 默认只列当前老师的，发现库里有别人的记忆时**明确报出来而不是静默过滤**，审计 A6 在同一记忆库里发现两个署名时判违约。

---

## 四 · 首次使用：不要求老师先做一次知识工程

普通文案老师不该为了用一个技能，先替 AI 完成一次知识库整理项目。这决定了三件事：

**目录还不存在时不卡住。** `bind --create` 按标准语料布局把目录建出来并写好配置，同时明确告诉老师三件不需要做的事——不需要先分好类、不需要改文件名、不需要清理旧文件。刚建出来的目录是空的，`bind` 会明确提示检索会返回空，避免他以为已经可以直接写稿。

**索引不追求一次分类正确。** 低置信度文件同时保留多个候选标签，不强行归类；只有某个模糊项会实际影响当前这一稿时，主智能体才在访谈里问。要求"先把知识库整理对，再让 AI 用"是个错误目标——没人有时间做这件事，于是这个前置条件会让整套东西停在第一步。

**绑定后的索引与写作流程只读源库。** `bind --create` 是老师显式授权的一次初始化写入，只创建空骨架和说明；后续不改写、重命名或移动语料。派生物落在项目内 `.blueink/`，其中只有 `index/` 可重建，`learning/` 与运行记录都有不可恢复的信息。

**老师直接给了参考文件路径时，不需要知识库。** "参考这两个文件写一篇"是完全正常的请求，为它索要一次绑定就是把五分钟的任务变成一次知识工程。这一次的启动回执会写明"无知识库·以附件为准"，让老师一眼看到本次没有品牌库参与——而不是以为品牌积累已经生效。

---

## 五 · 附件：授权与自主检索是两件事

绑定根约束的是**系统自己去找什么**。老师本次显式提供的附件是**他的授权**：他知道这份材料是什么、为什么给、要用在哪，来源强度高于任何检索结果。

把两者混成一条规则的代价是真实的：如果审计把老师给的库外文件判成越界，运行逻辑与审计合同就会互相打架——而这时候坏的不是其中一方，是那条把它们混起来的规则。

正确的切分：**附件不受绑定根限制，但必须登记**（绝对路径、内容哈希、品牌归属、`source: user`）。登记不阻止任何路径，它只让"老师给的"与"自己越界读的"在证据上可区分。没有它，审计只能二选一：要么放过真正的越界，要么把老师的授权判成违约。

**记内容哈希**是因为附件多半在知识库之外，事后可能被改动或移走。哈希让"当时读到的是哪一版"仍然可查——它是这三样信息里唯一事后无法伪造的。

**必须在同一次 `open` 里登记。** `task_attachments` 是本次证据边界的输入；事后补登记只能重开一次运行，前一次就成了废记录。

**已知代价，如实写出来：** 附件的品牌归属取自当前工作空间，不校验文件内容属于哪个品牌——校验它需要一张硬编码的品牌名表，而"新增品牌不改技能"是这套设计的承诺之一。所以老师如果附错了别家客户的材料，`open` 不会拦。拦它的是下游：来源核验员对正文做跨品牌实体核对，审计 A2 读它的结论。**入口不做内容判断，出口做**——这比在入口猜文件属于谁更可靠。

---

## 六 · 知识库里混进来的技能包不能反向控制流程

品牌知识库里出现别的写作技能包是常态：一个 `SKILL.md` 加若干 `references/*.md`，里面写着"第一段：行业背景"这类固定模板。把它们当经验证据检索出来，等于让外部提示词接管本次的编辑判断——而"结构固化"恰恰是这套系统要消灭的东西。

**判定按目录，不按文件名。** 目录里出现 `SKILL.md` / `AGENTS.md` / `CLAUDE.md`，则该目录整棵子树都标为 `instruction_artifact`。只按文件名过滤入口文件是不够的——**固定模板不写在入口文件里，写在它旁边的 `references/` 目录里**，只拦入口文件，模板照样会在"新闻稿 结构"这类检索里排到前列。

这些文件仍被索引（要能查到它们存在、在哪、有多少），但 `evidence_type` 记为独立类型、品类标签清空、默认不出现在 `retrieve` 结果里，返回值报 `excluded_instruction_artifacts` 计数。

**边界：这不是"经验总结不可用"。** 老师手写的对接经验、传播红线仍是编辑策略轨的一等证据。被隔离的只有由技能包生成的提示词与固定模板。

验证方式：把判定退化成"只按文件名匹配、不含子树"，回归测试立刻转红，其中一项直接显示固定模板重新排到检索结果第一位。

---

## 七 · 官方来源：白名单必须有代码读它

把允许的域名写进配置，然后在角色契约里写"只查已确认的官方渠道"，防不住任何有意或无意的偏差——"看起来像官网"的失败形态很多，而且都很像真的。

所以 `official.py` 是"什么算官方来源"的**唯一判定点**，每次联网取证前必须过这道闸，退出码非 0 就标成缺口。

匹配规则只有一条：主机名等于白名单域名，或是它的子域。

**写入时做归一化**——这是可用性与安全的平衡：接受完整 URL（老师会直接粘新闻中心链接），路径丢掉并告知；`www.` 前缀归一化到主域。不做这一步的代价是"官网也被拒了"，而那之后老师就会绕过白名单。

**白名单为空时一律拒绝**，并提示怎么补，而不是一律放行。

验证方式：后缀伪装、用户名伪装、带凭据、路径伪装、IP 直连、相似名、`file://`、`javascript:` 全部拒绝；apex、子域、大小写、尾点、http 全部放行。

**已知边界：** 白名单检查发生在访问前。网页工具若发生跨域重定向，需要研究员核对最终来源，不能只相信初始 URL。

---

## 八 · 角色边界：靠工具层强制，不靠自律

角色契约里写"你没有检索工具"，是否成立取决于怎么调用。走通用子智能体路径时，写作者拿到的是全套工具——那句话在这条路径下不成立，只能靠审计事后从 `read_paths` 发现越权。发现得到，但那时稿子已经写完了。

所以**原生子智能体注册是默认路径**：`install.sh` 默认把六个角色注册到 `~/.claude/agents/`，插件形态由 `agents/` 目录自动注册。工具白名单收到最小集：只有证据研究员有检索工具，其余五个角色都是 `Read, Write`。

**核验员为什么不给搜索能力。** 它有搜索能力就会在发现某句话没依据时，顺手去库里找一个来源把它圆上——于是"写作阶段无据发挥"这个真问题就此消失。

`validate.py` 把这条变成硬检查：写作者的 `tools` 里出现任何检索工具即 INVALID；每个角色都必须声明 `tools`。

**审查角色不读策略理由。** 给核验员 `program.json`（结论：事实边界、主线、舍弃项）而不给 `strategy.json`（理由）；编辑反方连 `program.json` 也不给——它判断的是"这篇稿子的信息权重合不合理"，不是"它有没有照计划执行"。理由会说服它们，而它们的价值恰在不被说服。

---

## 九 · 回执必须能落得下来

写审计材料本身如果成了主要耗时，那比不记账更糟——同时花掉了时间，又没换来可定位性。

所以回执有体量上限（`references/orchestration-protocol.md` 第四节那张表），**超限时的正确动作是舍弃，不是压缩字号**：一份 40 条事实的证据回执说明取证没有做取舍，而取舍本来就是它的工作。

**上限按真实量级取，不是拍的。** 一个达不到的上限等于没有上限——它只会被忽略，而被忽略的约束比不存在的约束更糟，因为它让人以为这件事已经被管住了。

**回执落盘是断点，所以产回执的调用一律放前台。** 后台调用一旦跑满运行时的等待上限被终止，回执一份都不会落盘，下一轮只能从头重跑。需要两个互相隔离的实例时（A/B 主线）前台顺序跑两次——隔离要求的是上下文互不可见，不是并发。顺序跑慢一点，但每一段都有断点。

**`read_paths` 里的路径必须绝对、或相对 `kb_root`，不许缩写。** 一条写残的路径事后无法解析，于是这条来源无法复核——而"来源可复核"就是核验员存在的全部理由。缩写只允许出现在交付给老师的来源清单里（那里的读者是人）。

**每份回执都要写 `read_paths`，包括正常情况下应为空数组的角色。** 空数组是一个断言（"我没有打开任何文件"）；缺这个字段则什么都没断言，审计无法区分"没读"和"读了没说"。

---

## 十 · 动态充分性：判据必须可核对

"访谈到足够理解"如果只是一个感觉，它就不构成停止条件：模型可能过早开写，也可能无限追问。

**判据是五个维度：事实安全、传播主线、信息权重、表达边界、交付可行性。** 下一问的答案仍有现实可能改变其中任一项时继续追问；否则记录剩余假设，进入写作。

**它们是判据，不是问卷。** 不要求每类任务都涉及全部五项，也不要求逐项确认——一条短社会化文案未必需要完整时间线，而 QA 更需要明确发言边界。要求所有任务满足同一套完整字段，会让访谈退化成表单，并诱导模型为了"填满门槛"追问大量不改变稿件的问题。

**表达边界最容易被漏掉，而它的返工代价最高。** 事实错老师一眼能看出来；语义温度不对、发言人身份用错、把媒体署名稿写成品牌自述——这些要读完整篇才发现，然后整篇重写。

**停止时必须指名评估的是哪一维。** 一句不指名的"信息已经足够"对任何一次访谈都成立，因此它什么都没说。审计 A3 把这一条变成机械检查：`stopped_because` 里认不出任何一个维度就判违约。

A3 另外守三条：每轮 `question` 只含一个问题（一次问三个，老师会挑最容易的答，剩下两个变成沉默，而你无法区分"他觉得不重要"和"他漏看了"）；归一化后相同的问题不许出现两次（重复提问会让老师开始敷衍，而敷衍的回答比没有回答更危险）；连续两轮 `changed` 为空判违约（说明在问无效问题，该停下重新定位缺口，而不是靠继续追问碰运气）。

**不显示信心百分比。** "当前信心 87%"无法校准，但它看起来像一次测量，老师会照着它决定要不要再补材料。审计把这类数值判为违约。

**零轮访谈是合法结果，不是违约。** 老师把交付合同和封闭证据边界都给全了的时候，一个合法问题都没有才是对的。判它违约会训练出"为了过审计随便问一句"的行为，而那比不问更糟。但必须写清为什么没问——没有理由的零轮和"根本没做访谈"在证据上不可区分。

### 有意不机械化的两条

**"带着判断提问"无法机械检查。** 一个问题是不是暴露了自己的判断、给了理由、说明了代价，这是语义判断。协议给了模板和四种坏形态（空问偏好、复述式确认、打包多问、无后果提问），但只有打包多问能被机械抓住。这一条靠协议，不靠审计——**如实承认，好过做一个只能测出关键词的假检查**。

**"问前查过索引"也不机械检查，这是刻意的。** 一个自报的 `checked: ["索引"]` 字段证明不了任何事：模型可以写上它而根本没查。所以审计守的是**结果**而不是过程——归一化后相同的两个问题直接判违约，因为那是"没查已有回答"唯一可被证明的后果。`checked` 字段仍然要求写，但它的读者是人：事后发现某个问题的答案本来就在索引里时，能一眼看出是没查还是查了没查到。

**这条切分可以推广：机械检查只应该守能被证明的结果，不应该守自报的过程。** 守自报过程的检查会得到一个副作用——模型学会填那个字段，而字段一旦被填满，人就再也无法从记录里看出真实情况。

---

## 十一 · 审计器自己也会误报，而误报比漏报更贵

误报会训练出为了通过审计而做的错误行为，并且最终让人学会整体忽略某一条检查项。三条从这里得出的规则：

**运行目录内的产物永远合法。** 策略师读 `evidence.json`、写作者读 `program.json`、反方读 `draft.md` 是编排协议规定的正常流程，不是"检索越过了绑定目录"。这条判断在 A2 与 A4 两处都需要，所以提炼成一个共用判据 `in_run_dir()`——**一条保证对一个位置成立、对另一个不成立，这类不对称最难发现**。

**在飞的运行不判违约。** "已经成稿却没有交付记录"这类检查只在运行归档后才成立；中途中断很常见，把一次正在进行的运行说成违约是纯粹的噪音。信息不丢——它们仍然出现在 `missing_artifacts` 里，结论落到 `incomplete`。

**判不准的判 `note` 而不是 `violated`。** 取证报了冲突而写作程序里没有 `assumptions`，可能是静默选边，也可能是老师在访谈里逐条裁决过（那时 `assumptions` 合法地为空），机器判不准。判提示而不判违约，是因为一次误报会让人学会忽略整条检查项，代价比这条漏网更大。

**构造的夹具验不出作者没想到的形态。** 夹具继承作者的盲区，然后用全绿把盲区盖住——多写几个也出自同一个脑子。所以夹具集合里保留一个**从真实运行脱敏而来的保留测试**，它不参与日常评分，以免被当成优化目标。

**夹具之间的契约相互独立。** 一项违约不能连带另一项，否则定位就退化成"哪儿都有问题"。

---

## 十二 · 学习：条件化记忆，不是规则清单

规则清单的失败模式是可预测的：它只增不减（没人敢删，因为每条都来自一次真实的批评）；越长越没人看（包括模型——规则挤在同一个上下文里，越靠后越失效）；最后每条规则都在某些场合是错的，而系统无法判断"这次算不算"。于是产生一个荒谬结果：积累得越多，表现越不稳定。

条件化记忆携带**触发条件**和**不适用范围**，因此可以被判断；携带**置信度**和**反例**，因此可以被修正。

四条纪律：

- **上限 0.9，永不到 1.0。** 任何记忆都可能在下一个场景里失效。一个饱和到 1.0 的置信度会让"再来一个反例"失去意义。
- **"独立任务"指不同的传播事件。** 同一场活动的三篇稿子里出现三次同向修改算一次证据，不算三次。不加这一条，一场大活动就能把一条偶发偏好推到高置信度。
- **独立任务必须指向一次真实运行。** `reinforce` 声明为独立事件时会校验对应的运行记录是否存在，不存在就拒绝。允许引用不存在的运行，等于允许凭空制造"多个独立任务中重复出现"——而置信度完全依赖这个计数。
- **反例不删旧结论。** 降低置信度，或给旧结论加一条"不适用于……"。冲突通常说明适用条件写得太宽，而不是原来的观察是假的。

**只有初稿与修改稿、没有理由时，只能记录变化本身，不许推测动机。** 看到老师把长句改短，写"老师偏好简洁表达"是编造——他可能只是这次篇幅超了。正确的记录是"第 2 段由 68 字改为 41 字，未说明原因"。

**方法论级的候选只记录，绝不自动改写通用方法论。** 任何工作空间都不允许自动修改 `SKILL.md`、`references/`、`agents/`。

---

## 十三 · 交付：审查报告没有天然价值

一份罗列"通过了哪些规则、调用了哪些子智能体、读取了哪些文件"的长报告，业务老师大概率不看。而一旦他学会跳过它，后面所有的质量信息都失效了。

所以交付的是**一张极短的核对卡**加一份**实际来源清单**：只列未通过项，通过项一条都不写（写了就等于让老师在一堆"√"里找那个"×"）；超过 5 条时按严重度取前 5 条，因为**列 12 条等于没列**。

结论行的口径来自核验回执，不允许主智能体自行放宽。

**同一份内容必须同时写入运行目录。** 没有它，事后无法区分"生成得不好"和"生成没问题但交付时漏了核对卡"——而这两种问题的处置完全不同。

---

## 十四 · 边界：不承担改稿编排

一次生成内部当然会回退，那些回退由回执 `status` 驱动，责任明确、终点确定。

**但"老师看完稿子提了修改意见，接下来退到哪一层"不由本技能负责。** 这里没有"修改意见性质 → 退到第几步"的对照表，也没有阶段回退命令。给出这张表就等于开始建设一个改稿操作系统：它需要版本、需要 diff、需要知道哪一段是上一轮哪个判断的产物，而这三样都没有。**半个改稿系统比没有更糟**——它会让人以为改稿这件事已经被管起来了。

老师的修改意见只有一个去处：学习外循环。这是"下次做得更对"，不是"这次退回去重做"。

`self_check.py --claims` 机械守着这条边界：子命令表里一旦出现 `rework` / `rollback` / `revert` 这类名字就判失败，并且每个子命令都必须在文档里出现过——**范围失控从来不是一次大动作，是一次加一个子命令。**

---

## 十五 · 做不到什么

一个诚实的设计必须说清它的边界。

- **文案质量没有证据。** 跑通的是编排与判断，不是稿子。状态层与审计夹具全过都不改变这一点——**审计器守的是形式契约**。稿子好不好只有文案老师能判断，需要在真实任务上盲评。
- **不能把低质量知识自动变正确。** 它能让错误可定位、可归因、可迭代，不能凭空提高证据质量。未经人工审核的一线素材参与生成，是明确选择过的取舍：取其精华，靠日常反馈反向迭代。代价是头几周的产出质量取决于素材质量。
- **不保证稿子一次就能用。** 第一质量指标是"老师把初稿改到可提交所需的修改量和修改时间明显下降"，不是"一次通过"。
- **不做品类认证。** 登记在册的品类是压力测试变量，不是验收边界。方法论对品类保持开放。
- **旁路检索是词项检索，不是语义向量检索。** 同义表达仍依赖研究员按需回读目录。
- **PDF、早期 DOC/XLS/PPT 格式和图片只登记元数据。** DOCX / PPTX 能读正文，这三类仍需老师在访谈中说明或先转成文本。
- **品牌集合层判定是结构启发式，不是语义判断。** 一个只有两个内容子目录、名字又不含语料特征词的正常单品牌库会被误判，需要 `--force`。选这条是因为误拦的代价（多一次 `--force`）小于漏拦的代价（整套稿子混品牌），但它确实会造成偶发摩擦。
- **品牌名匹配放宽到子串，因此会漏掉一类情形。** 绑定「理想」而本次说「理想科技」会被判为匹配（后者包含前者）。这是为可用性付的价：老师习惯说简称，把简称判成不匹配会让提示很快被当成噪音跳过。真正需要拦的"两个不同客户"通常互不为子串，所以这条漏网的实际概率低；但如果两个签约品牌确实存在包含关系，绑定时要在 `notes` 里写清，并在访谈中确认全名。
- **未绑定的附件模式不做跨品牌实体核对。** 没有品牌知识库就没有"这个品牌该出现哪些实体"的参照，来源核验员只能核对附件内的一致性。老师附错了别家客户的材料时，这一条兜不住——所以这个模式的启动回执会明确写出"无知识库·以附件为准"，让这次的边界一眼可见。
- **附件的品牌归属不校验文件内容。** 见第五节。
- **归因准确性无法机械校验。** 审计只能检查候选知识带了触发条件和反例、置信度没越界、归属只有一位老师，检查不了归因归得对不对。错的归因由置信度衰减吸收：拿不到重复证据的结论会在半年后衰减掉。
- **记忆库没有并发保护。** 两个进程同时写会丢一次更新。这是单人单机工具的取舍——加文件锁能防，但会为一个几乎不会发生的场景引入一套需要维护的机制。
- **子智能体调用耗时不可预测。** 单个角色调用从数十秒到二十分钟不等，这是宿主环境的性质，不是本技能能控制的，但它是"一次任务要跑多久"这个数字的主要不确定来源。
- **不承担 Word 排版、字体、配图、品牌文档模板，也不承担改稿编排与版本回退。** 第一版只负责文案生成。
- **不承诺自然语言自动触发。** 只认 Claude Code 显式入口；默认裸命令，同名冲突时用插件规范化入口。

最后一条最容易被当成缺陷，其实是选择：一个"有时会自动生效、有时不会"的入口，比一个必须显式调用的入口更难排查。**能被确认的行为，比看起来更聪明的行为更有价值。**
