# 技术设计说明

这份文件定义 BlueInk 的机械约束、执行点和可验证边界。运行方法见 `references/`，产品范围见 `SKILL.md`。

## 一 · 入口

根级 `SKILL.md` 设置 `disable-model-invocation: true`，默认入口为 `/blueink-suite`。插件不提供同名 commands 入口。

`open` 写入 `meta.json`，其中包含 `run_id`、`started_via`、品牌、证据边界和登记附件。审计 A1 以运行记录验证显式入口。

## 二 · 工作空间与品牌隔离

一个工作空间绑定一个品牌知识库。索引只扫描该根目录，并跳过指向根外的符号链接。

品牌集合层使用目录结构启发式识别；`brand_matches` 负责本次品牌与绑定品牌的核对；`workspace.within` 负责实际读取路径的归属判断。

用户附件可位于绑定根之外，但必须登记。附件登记包含绝对路径、SHA-256、字节数、品牌和 `source: user`。

附件品牌归属取自当前工作空间，`open` 不根据文件内容猜测品牌。已绑定任务由来源核验阶段检查跨品牌实体；未绑定附件任务只检查附件内部一致性。

## 三 · 知识库与索引

`bind --create` 创建标准语料骨架。索引与写作流程只读源库，派生数据写入项目内 `.blueink/`。

索引复用以内容哈希和稳定源路径共同判定。Schema 版本变化触发全量重建；同内容不同路径保留不同的来源身份。

目录中出现 `SKILL.md`、`AGENTS.md` 或 `CLAUDE.md` 时，整棵子树标记为 `instruction_artifact`，默认不进入检索候选。老师手写的经验总结和传播红线不属于该类型。

## 四 · 证据路由

封闭附件快线要求：登记附件正文可读、足以支撑交付、没有高影响冲突或时效问题，也不需要知识库、官方来源或额外风格样本。

快线由编辑策略阶段先写轻量 `evidence.json`，再写 `strategy.json`。扩展证据路径只围绕已命名的缺口检索，缺口闭合后停止。

事实、风格和编辑策略使用不同的检索轨道。事实看来源与日期；风格看同品牌、同场景、近期终稿；编辑策略看初终稿差异、修改意见和独立案例。

## 五 · 官方来源

`official.check_url` 是官方来源的唯一机械判定点。允许主机名等于白名单域名或属于其子域；后缀伪装、用户名伪装、IP、通配符、非 HTTP 协议均拒绝。

白名单写入时归一化为主域。联网取证记录 URL、获取时间和页面日期；跨域重定向后的最终来源由证据研究阶段核对。

## 六 · 单会话阶段执行

当前 `/blueink-suite` 会话按需读取 `references/stages/` 下的指导。插件根目录不包含 `agents/`。

阶段输入是推理合同，不是宿主权限隔离。成稿、来源核验、编辑红队和反馈归因禁止新增检索；`read_paths` 记录可观察的文件访问。

来源核验与编辑红队属于受限输入复核。共享上下文可能产生前序锚定，因此产品不把结果标记为独立评审或盲评。

## 七 · 阶段产物与恢复

阶段产物写入 `.blueink/runs/<run_id>/`。公共信封包含 `role`、`run_id`、`task_id`、`status` 和 `read_paths`。

回执先落盘再切换阶段。`doctor` 根据实际产物判断运行位置；缺失、损坏或 `blocked` 的阶段单独执行。

回执设置体量上限：证据事实不超过 25 条，策略不超过 220 行，写作程序不超过 120 行，写作回执不超过 40 行，红队攻击不超过 6 条，反馈候选不超过 8 条。

## 八 · 动态访谈

访谈停止判据包含事实安全、传播主线、信息权重、表达边界和交付可行性。下一问可能改变其中任一项时继续，否则记录剩余假设并结束访谈。

每轮只问一个问题，禁止重复问题。连续两轮 `changed` 为空表示问题没有推动稿件判断。零轮访谈允许，但 `stopped_because` 必须说明已经核对的维度。

## 九 · 条件化记忆

记忆候选记录 scope、证据强度、触发条件、不适用范围、反例和来源运行。置信度上限为 0.9。

独立证据必须引用真实运行；同一事件的多条反馈不计为多个独立任务。反例优先缩小适用范围，并保留原始观察。

记忆写入 `.blueink/learning/`。`index/` 可重建，`learning/` 不可从语料自动恢复。

## 十 · 审计与评测

审计包含五项契约：入口唯一、单品牌隔离、动态访谈、阶段边界和输出有效。

审计器只对已经进入的阶段判定违约；运行尚未到达的阶段标记为 `incomplete`。失败项必须定位到具体文件或字段。

状态层回归在临时目录执行绑定、索引、检索、附件登记、URL 校验、记忆升降和审计。变异门向技能副本注入 11 个高影响失败条件，并要求对应检查转红。

## 十一 · 产品化表达

用户可见文档、运行时阶段指导、安装说明和产品元数据统一使用当前时态，只描述现行能力、触发条件和边界。

`validate.product_voice` 对产品文档执行机械扫描；评审同时检查未命中关键词的非当前时态叙事。

## 十二 · 能力边界

BlueInk 负责文案生成，不承担改稿编排、版本回退、Word 排版、字体、配图、品牌文档模板或品类认证。

产品只支持 Claude Code，不承诺自然语言自动触发。插件要求 Python 3.9 或更高。

运行记录包含访谈、素材路径和正文，默认保留 90 天，并至少保留最近 20 次运行。`purge` 默认试运行，只有显式 `--apply` 才删除项目内运行记录，永不触及源知识库。
