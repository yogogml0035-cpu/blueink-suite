# 技术设计说明

## 一 · 入口与运行记录

根级 `SKILL.md` 设置 `disable-model-invocation: true`，并用 `disallowed-tools` 禁用 Skill、Agent、Task，避免显式入口被其他运行方法旁路。`open` 创建 schema 5 的 `run.json`，登记入口、品牌、知识库、附件路径与 SHA-256。schema 4 的 `run.json` 与 3.x 的 `meta.json` 继续只读兼容。

## 二 · 单品牌隔离

一个工作空间只绑定一个品牌知识库。老师附件必须登记但可以位于绑定根外；自主读取只能位于绑定根内。索引跳过越界符号链接和历史 `SKILL.md / AGENTS.md / CLAUDE.md` 指令产物子树。

## 三 · 单智能体正文快线

BlueInk 是本次唯一运行 Skill，不再加载其他 Skill、斜杠命令或品牌写作助手。默认只读取 `references/generate.md`。生成任务必须有方向确认，普通轮次不超过两轮，额外轮次只能是事实或来源硬冲突。明确附件的普通任务只先保存方向，随后一次成稿到 `delivery.md` 并执行 `handoff`；事实原子和完整编辑决策只在扩展研究时进入关键路径。研究、反馈与排障按条件读取。

## 四 · 一份正文与三份侧车

- `delivery.md`：老师唯一修改入口，也是最终正文；
- `run.json`：运行元数据、逐轮回答、方向和实际阶段耗时；扩展研究才增加最多 12 条事实原子和完整编辑决策；
- `verify.json`：同一智能体在正文交付后对高风险句的受限复核，只记录发现的问题；
- `delivery-check.md`：面向老师的交付核对卡与实际来源。

`save` 从标准输入解析 JSON 并原子写入。附件快线的方向与核验输入都是轻量结构；`handoff` 展示 `delivery.md`、记录正文哈希和阶段耗时，并把正文所有权交给老师。之后当前智能体不再改正文。`close` 只在正文仍与核验版本一致时生成 `delivery-check.md` 并归档；最终回复的第一个可见区块是完整正文，核对结论与侧车路径只能放在全文之后。

## 五 · 审计

新版审计仍使用入口唯一、单品牌隔离、动态访谈、阶段边界、输出有效五项契约。它检查唯一正文、三份侧车和结论自洽，不把通过解释成文案质量证明。schema 4 与 3.x 运行按原合同只读检查。

## 六 · 留存与学习

运行记录默认保留 90 天且至少保留最近 20 次。反馈才增加 `feedback.json`，候选记忆保留触发条件、不适用范围和反例；索引与写作流程不改源知识库。

## 七 · 能力边界

BlueInk 负责文案生成与来源核对，不承担改稿编排、版本回退、Word 排版、品类认证或自然语言自动触发。模型和 effort 不写入运行规格；阶段耗时只记录，不触发硬超时或自动降级。
