---
name: blueink-suite
description: >-
  蓝墨 BlueInk——证据驱动的汽车公关文案编辑决策系统。不是规则库：每次任务重新判断事实边界、传播主线、素材取舍
  和信息权重，动态形成一份只对本次有效的"写作程序"，再由同一智能体按阶段加载对应指导文档执行；反馈修正的是判断条件
  与证据权重，不是不断增加固定规则。用于新闻稿、媒体供稿、邀请函、传播指引、核心信息、演讲稿、QA、社会化文案、
  视频脚本、串词等公关文案生成。新工作空间绑定一个品牌知识库；绑定完成后的索引与写作流程只读源库。
  必须由 Claude Code 显式命令启动，默认入口 /blueink-suite。
license: proprietary
disable-model-invocation: true
compatibility: Claude Code only. Supports macOS and Windows with Python 3.9 or newer.
metadata:
  author: 新蓝标数字 · 汽车事业群 · AI 内容中台
  version: 3.0.0
  created: 2026-08-22
  last_reviewed: 2026-08-24
  review_interval_days: 90
  maintainer: 汽车事业群 AI 内容中台 · 文案岗
  provenance: 蓝墨平台四层架构、四品牌历史稿件资产、一线文案岗问题共识与对抗式审查判据
---

# /blueink-suite — 证据驱动的编辑决策系统

## 核心命题

**BlueInk 不是汽车文案规则库，而是一套证据驱动的编辑决策系统。** 它不把历史材料直接当规则，而是根据当前任务重新判断事实边界、传播主线、素材取舍和信息权重，动态形成一份只对本次有效的"写作程序"，再由同一智能体按阶段加载对应指导文档执行。反馈修正的是判断条件与证据权重，而不是继续增加固定规则。

规则库这条路走不通，原因不在规则数量不够，而在于它把一线素材当成了可直接执行的命令。**决定稿件质量的从来不是"套哪个模板"，而是：写什么、不写什么、为什么、各部分分配多少笔墨、采用哪条叙事主线。** 全部机制都服务于把这五个判断做对，并在做错时能被定位到具体阶段和具体文件。

## 五条方法论原则

1. **材料是证据，不是命令。** 初稿、终稿、客户反馈、官方资料、经验总结都只有特定的证据强度、时效和适用范围。历史成稿不能覆盖新事实；查不出依据的事实性表达等价于杜撰。

2. **编辑判断先于表达。** 先决定写什么、不写什么、为什么、分配多少笔墨、走哪条主线，再进入成文。表达问题只有在判断已经固定下来之后，才可能被独立归因。

3. **品类规定任务，不规定结构。** 品类只说明这份内容必须完成什么传播任务、不能越过什么边界，不预设段落顺序。参考稿是候选结构，只有与本次任务匹配时才启用。品牌同理——品牌调性由当前可用的品牌证据决定，不是一组形容词。

4. **只解决会改变当前稿件的不确定性。** 下一问的答案仍有现实可能改变**事实安全、传播主线、信息权重、表达边界或交付可行性**时才继续追问；否则记录剩余假设，直接进入写作。已经清楚的事不再确认，已有结果的环节不再重跑。这五项是判断"问下去还有没有意义"的维度，不是要逐项问完的清单。

5. **结果反向校准判断，而不是制造规则。** 修改意见、A/B 选择和反例修正的是适用条件、证据权重和决策策略。一次修改不能变成永久纪律——禁令会越来越多，判断力不会。

## 决策主干：有向、可跳过、可局部回退

七个责任节点之间有**不可颠倒的因果依赖**，但它们不是七个必须逐项执行的步骤，更不是七项检查清单。

```
传播任务 → 证据边界 → 编辑判断 → 本次写作程序 → 成稿 → 双重审查 → 交付与学习
```

因果关系本身就是理由：没有传播任务，无法判断哪些证据相关；没有事实边界，形不成可靠主线；没有素材取舍和信息预算，写作者没有可执行输入；没有正文，核验与反方没有审查对象；没有真实反馈，无法更新未来判断。

每个节点允许三种动态行为：

- **跳过**——已有可靠结果时不重复处理。老师已经说清的不再问，附件已经给足的不再检索，上一轮已经落盘的回执不再重跑。
- **条件分支**——只有真的出现时效问题、来源冲突、两条实质竞争的主线或高影响推断时，才触发官方取证、A/B 或决策卡。默认路径上没有这些动作。
- **局部回退**——事实问题退回证据节点，主线问题退回编辑判断，执行偏差只退回写作。**不重跑整条链。**

**不要为这七个节点生成状态表、进度表或决策域清单。** 它们是当前智能体自己的因果地图；把它渲染成表格交给老师，就是把方法论重新写回操作手册，而且会立刻吃掉本该用于判断的注意力。

## 第一件事：本次的证据从哪来

每次启动先确定这一条，因为它决定后面所有检索的边界。三种情形，判断顺序固定：

下文用 `$BLUEINK` 代指**已经按当前电脑解析并试跑过的** `blueink.py --project <当前项目根>` 命令；它不是让老师原样输入的跨平台命令。macOS 通常以 `python3` 开头，Windows 通常以 `py -3` 或 `python` 开头。

**一 · 老师给了明确的参考文件路径。** 那就是本次的证据，不需要品牌知识库。直接登记，先只用这几份成稿：

```bash
$BLUEINK open --mode 生成 --brand "<本次品牌>" --attach "<文件绝对路径>" [--attach …]
```

**有附件时不要先跑 `status`、`bind`、`check-brand` 或 `index`。** `attachments` 非空已经决定本次走情形一；先检查工作空间只会把一个合法的附件任务误渲染成“未绑定错误”。直接在同一次 `open` 里登记全部附件。

未绑定知识库的项目也能这样开始——"参考这两个文件写一篇"是完全正常的请求，为它索要一次绑定就是把五分钟的任务变成一次知识工程。缺到无法成稿时向老师要，不要自己去找。

**二 · 项目还没有绑定知识库。** 先跑 `status` 确认，然后在访谈里问出知识库位置，一次问一件，问完再写配置：

```bash
$BLUEINK status                      # 正常返回；读取 bound 判断是否绑定
$BLUEINK bind --brand "<品牌>" --kb "<知识库目录绝对路径>" \
              [--official "<官网>"]
$BLUEINK index
```

**老师说"还没有知识库目录"时不要卡住，加 `--create`**：它会按标准语料布局把目录建出来，并写好配置，老师之后把稿件丢进去就行。**不要求他先分好类**——分不清放哪一类就随便放，索引会给候选标签。

**三 · 已绑定，但本次要写的品牌可能不是绑定的那个。** 访谈里确认品牌之后核对一次：

```bash
$BLUEINK check-brand --brand "<本次品牌>"
```

不匹配时**不要自己选边，也不要继续**。用当前知识库给另一个品牌写稿，会把别家客户的表达和事实带进这一稿，而这类错误在成稿里看不出来。把三条出路交给老师选：本次品牌名写错了、换到那个品牌的项目目录去、或本次只参考指定文件不用知识库。`open --brand` 会在开启运行时再挡一次，所以品牌名一律传给它。

## 证据边界：附件与绑定根的关系

**老师本次明确提供的附件，就是本次任务的证据。** 不论它是否落在绑定知识库根内——它由老师显式指定，来源强度高于任何检索结果。

- 老师说"以附件为准"时，附件**默认构成本次的封闭证据边界**：先只用附件完成任务。
- 只有附件存在**会阻止成稿的高影响缺口或冲突**时，才按最小范围查询绑定知识库，并写清缺的到底是什么。
- **不为"可能有用的风格参考"主动展开整库研究。** 整库扩张的代价不只是慢，是把一个附件驱动的任务变成一次知识库普查。
- 绑定品牌根约束的是**系统自主检索的范围**，不是老师的授权。跨品牌污染靠"自主检索根只有一个"来防，不靠拒绝老师给的文件。
- **关的是事实边界，不是风格样本。** 附件里通常没有"这位客户实际采用过的表达"，而那只能从绑定库里的终稿取。允许取少量风格样本，按品类和时间近选，记进 `style_refs` 而不是 `facts`。

附件必须登记进本次运行（绝对路径、内容哈希、品牌归属、`source: user`），否则审计无法区分"老师给的"与"自己越界读的"。登记方式见《工作空间与索引》。

## 运行底线

只有五条，跨全流程适用；其余全部按需读取。

1. **只由 Claude Code 显式入口启动。** 默认使用裸命令 `/blueink-suite`；如果另一插件占用了同名裸命令，Claude Code 的规范化入口是 `/blueink-suite:blueink-suite`。两者指向同一个 Skill。frontmatter 的 `disable-model-invocation: true` 关闭模型自动触发。
2. **一个工作空间只绑定一个品牌知识库。** 换品牌就换项目；当前品牌在换电脑或移动知识库后可以迁移路径，但必须重建索引。`bind --create` 会显式创建空知识库骨架；此后的索引与写作流程不改写源库。
3. **一个智能体分阶段执行。** 不调用 Agent、Task、子智能体或独立 `claude --agent` 进程；进入每个阶段时只加载对应指导文档，并先落阶段产物再继续。正文只能在成稿阶段写，交付阶段不偷偷重写。
4. **事实必须有来源；冲突显式上交老师裁决，不静默选边。** 静默选边把一个可见的分歧变成一个不可见的错误。
5. **反馈先成为带条件的候选证据。** 一次修改不直接变成规则；判不准归属时归到更窄的范围，不是更宽的。

确定性工作一律交给脚本而不是模型判断：文件扫描、哈希、增量索引、路径校验、URL 白名单、运行记录、契约审计。需要这些能力时，**Run（运行）** `${CLAUDE_PLUGIN_ROOT}/scripts/blueink.py`；Claude Code 会把 `${CLAUDE_PLUGIN_ROOT}` 与 `${CLAUDE_PROJECT_DIR}` 替换为当前机器的原生绝对路径。不要把作者电脑或安装缓存路径写死进阶段切换卡。

```bash
# macOS：python3；Windows：优先 py -3，找不到再试 python
<Python 3.9+ 命令> "${CLAUDE_PLUGIN_ROOT}/scripts/blueink.py" \
  --project "${CLAUDE_PROJECT_DIR}" open --mode 生成 --brand "<本次品牌>" \
  [--attach "<附件绝对路径>"]
```

第一次运行先确认当前平台的 Python 命令；之后把**实际解释器、技能根、项目根**写进当前运行与阶段切换卡。老师带了附件时在同一次 `open` 里登记，不要先开运行再补。
调用 `open` 时用 `--started-via` 如实记录用户实际输入的上述显式入口；没有同名冲突时默认值就是 `/blueink-suite`。

`blueink.py` 是状态与记账层，**不是一条会把流程跑完的管线**：它没有"跑完就出稿"的命令，也不决定下一步做什么。真正的流程由当前智能体按访谈结果和证据缺口分支。

**不要 `cd` 进技能目录后省略 `--project`。** 工作空间默认从当前目录向上查找；在技能目录执行会解析不到真正的项目根。显式传入当前机器的项目根绝对路径即可避免该问题。同理，**附件要在开启运行的那一次就登记**：`task_attachments` 是本次证据边界的输入，事后补登记只能重开一次运行，前一次就成了废记录。

## 认知责任按阶段分离

拆分依据不是品牌，也不是品类，而是**无法互相替代的认知责任**：取证、编辑判断、写作、事实核验、编辑红队、反馈归因。按品类拆会重新制造固定结构，按品牌拆会重新制造品牌技能包。

这些责任不再对应六个 Agent 实例，而是同一智能体的六个阶段。每次进入阶段只加载该阶段指导文档，并用 `evidence.json`、`strategy.json`、`program.json`、写作回执与审查回执形成断点。上下文和工具在会话中共享，因此来源核验与编辑红队是受限输入下的复核，**不宣称独立上下文或盲评**。

阶段输入、禁止动作、产物字段、切换顺序和局部返工规则全部在《单智能体阶段执行协议》里。进入六个阶段中的任何一个之前先读它。

## 按需读取

不要一次全读。按当前正在闭合哪个判断，只读对应那一份。

| 现在要做什么 | 读 |
|---|---|
| 首次在这个项目里运行；问出知识库路径或建一个；本次品牌与知识库不匹配；登记附件；索引、官方来源白名单、留存清理 | `${CLAUDE_PLUGIN_ROOT}/references/workspace-and-index.md` |
| 进入访谈；不知道下一个问题该问什么；判断还该不该继续问 | `${CLAUDE_PLUGIN_ROOT}/references/interview-protocol.md` |
| 取证；三轨分别看什么；冲突怎么处理；什么时候才允许联网 | `${CLAUDE_PLUGIN_ROOT}/references/evidence-tracks.md` |
| 判断这份交付物必须完成什么传播任务 | `${CLAUDE_PLUGIN_ROOT}/references/category-boundaries.md` |
| 进入证据、策略、成稿、核验、红队或反馈阶段；阶段输入与产物；禁止动作和局部返工 | `${CLAUDE_PLUGIN_ROOT}/references/stage-execution-protocol.md` |
| 成稿后；核对卡、来源清单、A/B 呈现、结论口径 | `${CLAUDE_PLUGIN_ROOT}/references/delivery-contract.md` |
| 收到老师反馈；判断某条记忆能否自动参与写作 | `${CLAUDE_PLUGIN_ROOT}/references/conditional-memory.md` |
| 稿子不对要定位到具体阶段和文件；审计结论怎么读；本地环境的反直觉事实 | `${CLAUDE_PLUGIN_ROOT}/references/troubleshooting.md` |
| 判断某项设计是否违背核心命题 | `${CLAUDE_PLUGIN_ROOT}/references/methodology-core.md` |

**能力边界：**负责文案生成，不做 Word 排版、字体、配图、品牌文档模板，也不建设改稿编排与版本回退。

## Prerequisites（运行前提）

- Claude Code，且必须显式输入 `/blueink-suite`；普通自然语言不自动触发。
- Python 3.9 或更高。macOS 通常运行 `python3`，Windows 优先运行 `py -3`，找不到再运行 `python`。
- 本次输入至少满足一种：已经登记的附件，或当前项目已绑定且可读取的单品牌知识库。

## Gotchas

- **给了附件时不要先运行 `status`、`bind`、`check-brand` 或 `index`。** 在同一次 `open --attach` 里登记全部附件；否则合法的附件任务会被误导进知识库流程。
- **未绑定时 `status` 仍正常退出。** 读取 JSON 的 `bound: false`，不要把退出码 0 当成“已经绑定”。
- **阶段指导不是独立 Agent。** 同一智能体共享上下文和工具；来源核验与编辑红队只能做受限输入复核，不能宣称独立或盲评。
- **根级 `agents/` 会重新开启子智能体注册。** 当前结构只允许读取 `references/stages/` 下的阶段指导。

更多环境反直觉事实与修正方法，**Read（读取）** `${CLAUDE_PLUGIN_ROOT}/references/troubleshooting.md`。
