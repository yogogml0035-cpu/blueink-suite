#!/usr/bin/env python3
"""``blueink.py`` 的别名，存在的唯一理由是技能打包规范的入口约定。

技能规范的管线检查器按固定文件名 ``scripts/run_pipeline.py`` 判断"这个技能有没有
单一入口"。而这套系统的确定性工作是一个**状态与记账层**，不是一条会把方向确认、
成稿和高风险复核机械跑完的管线——真正的流程由当前智能体按访谈结果和证据缺口分支，
硬套一个管线名字会让人以为存在一个"跑完就出稿"的命令。

所以实现全部在 ``blueink.py`` 里，文档和人都用那个名字；这里只做转发，不复制任何
逻辑。**不要往这个文件里加功能**：两个入口各写一半，等于把"入口只有一个"这条约束
拆成两处，而它的全部价值就在于只有一处。
"""

from __future__ import annotations

import sys

from blueink import main

if __name__ == "__main__":
    sys.exit(main())
